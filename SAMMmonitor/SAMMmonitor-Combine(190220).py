"""
SAMMmonitor-combineFunction.py
Purpose: SAMMmonitor function to combine and export hit list as combined XY-scatter values
"""

import os
import sys
import csv

""
#variable dictionary
data_folder == APEX3D data location
targets_csv == Targets CSV location

def readHitList(csv_file, delimitchar=',', headers = True):
        """ Reads hit list CSVs and combines duplicate entries
        Arguments:
        csv_file {str} -- [full str path to .csv file]
    """



#range 
for i in hitList:
    for formula in formulaCol
        if name = name
            csv.append(sum x in col y)